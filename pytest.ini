[pytest]
minversion = 8.3
addopts = --strict-markers -ra
testpaths =
    tests
pythonpath =
    .
python_files = *_test.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: quick smoke tests
    integration: tests requiring integration
    slow: tests that are slow